# 流行音乐主旋律自动生成:从民歌复刻到现代流行语料(项目报告)

> 本报告沿用一条“研究主线”:动机 → 问题定义 → 现有工作为什么不够 → 我们的方法 →
> 评测方式与提升幅度 → 复盘中遇到的困难与解决。这是对 v1/v2(德国民歌)的“换域”项目:
> **同一套 LSTM 单旋律生成技术,换到现代流行音乐语料**,并为此新写了一条
> “从整轨 loop 抽取主旋律”的预处理管线。所有实验在 Windows + CPU + 小规模数据上完成,
> 数字用于说明“相对改进的方向与量级”,不等同于论文级 benchmark。

---

## 0. 项目一句话

先用 v1/v2 把经典的“RNN-LSTM 旋律生成教程”从德国民歌跑通并做了小创新,再把这个项目
的落脚点换成**流行音乐自动生成**:换一个适合的流行语料库(Pop-K,30 万条 8 小节现代流行
loop),自己解决“loop 不是干净主旋律”的数据难题,重新训练出流行风主旋律模型,生成可听的
MIDI,并用统一评测量化和上一版采样器的差距。

## 1. Motivation:为什么做这件事

- 前两版在 Erk 德国民歌(KERN 语料)上验证了“单层/双层 LSTM 学会续写旋律”,但**民歌 ≠ 流行**:
  节奏型、音区、乐句长度、和声感差别很大。真正想做“流行音乐自动生成”,就必须先换一个
  流行语料,而不是只调模型。
- 换语料在公开教程里常被一笔带过(“换 data 目录就行”),但真实情况是:流行 MIDI 来源杂、
  质量参差、很多是“整段伴奏 loop”,**没有现成的干净主旋律轨**。这正好逼我们做一次完整的
  “领域迁移 + 数据工程”,比再调一层网络更有价值。
- 我们希望回答三个问题:流行语料去哪找、怎么把它洗成模型能吃的单旋律、同一模型在流行域
  上生成质量如何量化。

## 2. 问题定义:解决什么领域、什么问题

- 领域:**符号音乐生成(symbolic music generation)** 中“现代流行主旋律(lead melody)的自回归续写”。
- 输入输出沿用 v1/v2 的符号编码:整数 = MIDI 音高,`r` = 休止,`_` = 延音,`/` = 段落分隔。
  任务是语言建模式的:**给定最近 64 个时间步(4 小节 @0.25 拍),预测下一个符号**。
- 数据单元从“一首完整民歌”换成“一条 8 小节现代流行 loop”(Pop-K 的标准粒度)。
- 评价目标:预测要准(模型学到流行语料规律),生成要不复读、能自然停笔、音高有变化。

## 3. 语料调研:为什么选 Pop-K(以及“为什么不用 MP3 扩数据集”)

### 3.1 候选对比

| 语料 | 内容 | 下载 | 结论 |
| --- | --- | --- | --- |
| POP909 | 909 首真实流行歌,官方 MIDI 内单独标出 MELODY 人声主旋律轨,质量最理想 | 官网要表单登记;HF 镜像多数 gated/空 | 一时拿不到,弃 |
| MetaMIDI / LMD | 量大(几十万首)但风格杂、无干净主旋律轨 | 需网页点单 | 风格不好控,弃 |
| **Pop-K 300k** | 305,815 条 8 小节现代流行 loop,120BPM、96tick 统一,来源 Patchbanks 现代流行素材 | Zenodo 直链,53.6MB | **采用** |

Pop-K 是“受约束的现代流行域”:所有样本都是 8 小节、统一节拍、统一速度、统一来源风格,
正好命中“流行主旋律生成”的训练域。它还自带扩展:30 万条来自 base(人声主旋律+和弦+贝斯)
的数据增强,官方声明约 1360 小时总时长。

### 3.2 为什么不用 MP3/WAV 转数据

模型吃的是**离散符号序列(音高/时值)**,不是波形。MP3→符号需要“自动扒谱(AMT)”:
流行歌里人声、和声、鼓、贝斯混在一起,当前 AMT 在主旋律提取上的错误率仍较高——
错音、错节奏、混入和声,会把模型教坏。所以本领域惯例是**直接使用已经带“干净主旋律轨”的
MIDI 语料**,而不是用音频做端到端。这也是为什么候选锁定 POP909 / Pop-K,而不是去 mp3
网站抓歌。(真要“听感/扩数据”,正确做法是符号层的数据增强,例如移调/变速/节奏量化。)

## 4. 现有工作为什么“没直接解决”

| 方案 | 能做什么 | 为什么还不够 |
| --- | --- | --- |
| 直接拿 POP909 训练 | 主旋律干净 | 下载门槛高,本机复现不了 |
| 把 Pop-K loop 当“单音轨”直接编码 | 看起来省事 | **错**:我们统计抽样发现这些 loop 是“低音+和弦+主旋律叠在同一条轨”的多声部织体,直接编码会同时出现多个音高 |
| 只抽“同刻只有一个音”的文件 | 得到真·单音 | 抽样 1500 个全部存在跨时叠置/和声长音,能用的比例极低,等于放弃语料 |
| 直接跑 v2 旧民歌管线 | 代码现成 | 需要新预处理,旧管线只会解析干净单旋律 |

结论:**这个语料库的可用性取决于我们自己能不能把“主旋律层”洗干净**,这是本项目的
数据侧核心工作量。

## 5. 我们的方法:如何解决

### 5.1 预处理:主旋律层抽取(本项目的主要新代码)

对每条 8 小节 loop,逐音符 onset 顺序做如下判定:

1. 统计当前“仍在响”的所有音(含已判定为主旋律的音),取最高音 M;
2. 新落下的音里只可能有一个成为主旋律:取最高音 T,要求 **T > M**(必须高于当前一切已响的音);
3. 若这个 onset 是**和弦齐奏**(同一时刻多个音),再要求 **T 与次高音相差 ≥ 6 个半音**,
   否则把它当成和弦顶音丢弃;
4. 若新的主旋律落在前一个主旋律还没结束的位置,把前一个截断 → 得到严格单声部主旋律。

效果:以 7909 个候选为样本,洗出 7000 条主旋律(88.5%),平均每条 9~20 个音符,
音区自然集中在流行主旋律常用的中高音区。

### 5.2 编码与归一(让数据更好学)

- **0.25 拍量化**:把音符边界对齐到 16 分音符网格(和 v2 的时间步一致);
- **Krumhansl 调性归一**:语料没有 key signature,用音高类直方图与 Krumhansl-Kessler
  模板做相关,把所有旋律移到 C 大调 / A 小调;
- **八度归一**:同一条乐句在语料里常以不同八度出现(峰值词表一度到 54 个音高号),
  按旋律中位数整八度搬移后,词表从 57 降到 **49**,模型不再为“同样的音程换个八度”重复记参数;
- 过滤极端音域与过短/过长片段,输出与 v2 完全兼容的 `pitch/r/_//` 序列。

### 5.3 模型与训练(复用 v2 架构)

- Embedding(49→64)→ 双层 LSTM(128×2,带 Dropout)→ softmax,sparse CE;
- 24,000 条滑窗训练 + 4,000 条**模型完全没见过的**窗口做验证;
- EarlyStopping(patience=3,恢复最佳权重)+ ReduceLROnPlateau;实际跑 29 个 epoch,
  在 epoch 26 早停;
- 没有 GPU,全部 CPU 训练,单 epoch 约 30 秒,整轮约 15 分钟。

### 5.4 生成(复用 v2 采样器)

- naive 对照 = v1 做法(纯温度采样);
- v2 = 温度 + top-p(nucleus)+ 对最近 8 步符号的重复惩罚;
- 不强制结束,采到 `/` 就自然停笔。产出 `pop-mel-1~6.mid`(v2)与 `pop-mel-naive.mid`。

## 6. 评测结果与提升幅度

### 6.1 预测能力(模型指标)

| 指标 | 值 | 说明 |
| --- | --- | --- |
| 未见窗口 val loss | **0.4410** | 窗口区间 [24000, 28000) |
| 未见窗口 val acc | **0.8670** | 随机猜测基线 ≈ 2.04%(词表 49) |
| 词表 | 49 | 38(民歌 v1/v2)→ 49(流行域) |

### 6.2 生成质量:同一模型,naive vs v2(16 条种子、单次上限 220 步)

| 指标 | naive | v2 | 相对变化 |
| --- | --- | --- | --- |
| 自然结束率 | 1.00 | 1.00 | 持平 |
| 平均续写长度 | 47.6 | 40.1 | -15.8%(更早收束) |
| 8-gram 重复率 | 0.7089 | 0.4700 | **-33.7%** |
| 最长连续重复串 | 35.2 | 20.2 | **-42.6%** |
| 新音符个数 | 2.7 | 4.8 | +76.7% |
| 音高类别熵 | 0.432 | 1.052 | **+143.3%** |
| 音高跨度(MIDI) | 1.3 | 2.9 | +123.8% |

解读:纯温度采样在这个流行语料上会“卡在同一个音上反复拉长音”(new note 只有 2.7 个、
音域 1.3 个半音),加了 top-p + 重复惩罚后,旋律能走起来,重复率明显下降、音高多样性和
音域显著提升,并且仍能自然结束。v2 系列改进在民歌域成立,在**换到流行语料后依然成立**,
说明改进不是过拟合民歌。

### 6.3 现有局限(诚实边界)

- 生成的旋律偏“小音程游走”,音域和节奏型比真实流行歌单调——这是因为模型只看到单条
  主旋律、看不到和声进行,且语料来自 8 小节 loop 的均匀拼盘;
- 平均续写长度偏短(常在第 2~4 小节自然停),缺少“完整 8 小节+副歌”的宏结构;
- 下一步方向:把和弦轨一起编码做**和弦条件生成**,或引入结构 token / Transformer。

## 7. 复盘中遇到的困难与解决(重要)

1. **沙箱限制,写不进 D 盘**:用户原本希望在 `D:\Code` 下建项目,但当前环境只允许写
   家目录,项目最终落在 `C:\Users\lzq13\pop-melody-gen`,代码设计为整目录可搬移
   (路径全部相对 `__file__`),拷到 D 盘即可原样运行。
2. **流行语料“看似免费,实则难拿”**:POP909 官网要登记,多个 HF 镜像不是 gated 就是空目录,
   还有一个 50MB 的“pop909”tar 解压后是别人模型的 token 数据而不是 MIDI。逐个验证后,
   改用 Pop-K 的 Zenodo 直链,一次成功。教训:调研语料不能只看 README 描述,要实际下载
   抽查文件结构。
3. **music21 解析问题**:读 MIDI 时 `converter.parse(BytesIO)` 会拿工作目录当文件路径报
   PermissionError;先写临时文件再 `format='midi'` 解析可行,但更彻底的办法是换成轻量
   `mido` 解析(还能直接拿到 tick/通道信息)。
4. **30 万文件太大不能乱解**:直接随机读 gzip tar 里的文件非常慢(每读一个都要从头解压),
   1500 个样本几分钟跑不完;先花 1.5 秒解成普通 tar,再随机读单个文件,速度提升上百倍。
5. **“这不是旋律,是一条轨装的整段织体”**:这是本项目最大的数据坑。抽样统计显示所有样本
   都存在声部叠置(50% 同刻齐奏,100% 有跨时叠置),低音能低到 MIDI 24。我们写了
   主旋律层抽取规则,并用肉眼抽检多首(高音主旋律 + 块状和弦 + 低音的结构清晰可分离)。
6. **没有 key signature、时值不规整、跨八度重复**:分别用 Krumhansl 相关移调、0.25 拍
   量化对齐、整八度归一解决,词表从 57 缩到 49。
7. **CPU 训练慢/过拟合风险**:单 epoch ~30 秒、整轮 ~15 分钟可接受;训练曲线后期 loss
   出现平台甚至反弹,靠 EarlyStopping + 降低学习率把最佳权重恢复到 epoch 26。
8. **“loss 出 NaN”这类训练事故的预防**:本项目没踩到,但经验是:embedding 词表或学习率
   过大时最容易爆;用 Adam + 早停 + ReduceLROnPlateau、把输入归一化、必要时 clipnorm 兜底,
   并盯验证 loss(不是只盯训练 loss)。

## 8. 复现步骤

```powershell
# 环境(复用 v1/v2 venv, 内含 TF/Keras/music21/mido)
& "C:\Users\lzq13\melody-rnn-lstm\.venv\Scripts\python.exe" preprocess.py
& "C:\Users\lzq13\melody-rnn-lstm\.venv\Scripts\python.exe" train.py --train-samples 24000 --val-samples 4000 --epochs 40
& "C:\Users\lzq13\melody-rnn-lstm\.venv\Scripts\python.exe" gen_more.py
# 评测见 README.md 的命令
```

原始语料 `data/popk_300k_mid.tar.gz` 保留在项目里(53.6MB),预处理脚本会自动扫描、
抽取并生成 `dataset/ file_dataset mapping.json`;`model.keras`、`pop-mel-*.mid` 即为
本项目成果文件。

---

## 9. v3 正式实验与最终数字(在换域 + 修数据划分之后的“认真版”对比)

### 9.1 协议(为什么这版数字能信)
- 数据:dataset_v3 = 7,000 条“单声部人声主旋律 + 逐 token 和弦标签”(词表 49, 和弦 25);
- **按歌划分** 5600/700/700:同一首 loop 不会横跨 train/val/test(修掉 v1/v2 里“验证窗口和训练数据挨在一起”的泄漏);
- 训练:每个 epoch 从整个 train split 随机抽 20,000 条窗口;验证/测试窗口**固定缓存**,所有模型共用同一批 → 指标可比;
- 模型:Markov-6(add-1+backoff)、base LSTM、cond LSTM(旋律+和弦双流)、它们的 note-reweight 版本、2 层 Transformer;全部 CPU 训练,30 epoch 上限 + 早停。

### 9.2 逐符号结果(同一 4,000 条固定测试窗口;所有模型都按无权重 CE 评测)

| 模型 | test NLL | test acc | PPL | note-onset top-1 |
|---|---|---|---|---|
| Markov-6 | 0.9157 | 0.7655 | 2.498 | — |
| base LSTM(vanilla) | 0.8089 | 0.7520 | 2.245 | 0.027 |
| cond LSTM(vanilla) | 0.8245 | 0.7510 | 2.281 | 0.017 |
| **base LSTM + note-reweight(λ=6)** | 0.9400 | 0.6595 | 2.560 | **0.279** |
| **cond LSTM + note-reweight(λ=6)** | 0.9426 | 0.6607 | 2.567 | **0.303** |
| Transformer-2L(vanilla) | **0.7830** | 0.7545 | **2.188** | 0.070 |

解读:
- 单看“下一个符号的 NLL”,Transformer 最好,vanilla LSTM 其次,Markov 再次——说明把窗口做长、做对偶语义建模有用;
- 但 vanilla 模型在 **note-onset 位置几乎从不输出音符**(2~3%,接近随机):它用“永远猜 _ 和 /”把整体 loss 压得很低。这是网格化旋律 + 类别极度不平衡下的典型“懒惰解”;
- note-reweight 把“音符启动”的样本权重提到 6,note-onset top-1 从 ~3% 升到 28~30%(和弦版最高),代价是无权重 test NLL 上升约 0.13(因为模型开始“敢于冒险”输出音符,在延音/休止上的过拟合概率被摊薄)。这解释了为什么**只看 NLL 会严重低估这种模型的生成能力**。

### 9.3 生成质量(12 条 test prompt、temperature 0.7、C–Am–F–G)

| 模型 | 平均续写长度 | 合计音符数 | 自然停笔率 | 8-gram 重复率 | 音高熵(bit) | 音域(半音) |
|---|---|---|---|---|---|---|
| base LSTM(vanilla) | 12 | 25 | 1.00 | 0.125 | 1.18 | 2.5 |
| cond LSTM(vanilla) | 4 | 6 | 1.00 | — | 0.40 | 0.4 |
| base LSTM(note-reweight) | 25 | 135 | 1.00 | 0.014 | 1.60 | 6.2 |
| **cond LSTM(note-reweight)** | **76** | **426** | 0.75 | 0.094 | **2.02** | **9.9** |

结论(如实):
- 没有和弦时,模型写一小段就自然收束;给定同一组和弦,note-reweight 的**双流模型**能连续写 ~3 倍长、音符多 ~3 倍、音域更宽的旋律——和弦在这里相当于给了模型“接下来往哪走”的结构信号;
- 严格“音符落在所在小节和弦三音内”的指标上,两个重平衡模型都接近音阶贴合水平(25%~27%,真实人声为 36.8%,随机为 25%),**该项没有显著差异**——所以我们的主线论述是“可控制和连续性”,不是“更准地落在和弦音上”(后者受音阶内经过音影响,区分度低);
- 听感上,note-reweight 版明显比 vanilla 版“会唱”:前者每个样例有几十个新音,后者往往只剩长音和休止。

### 9.4 这轮又踩到的坑(v3 追加)
1. **早停指标和训练目标不一致**:一开始 note-reweight 训练仍用“无权重验证 loss”做早停,模型在第 6 epoch 就被误停(权重 loss 还没收敛);修复:验证集用同一套样本权重选早停,测试集仍用无权重 CE 出报告数字。
2. **“生成不出新音”比“loss 高”更致命**:用 per-GT 准确率拆开后才发现 vanilla 模型的 0.75 整体 acc 里 99% 来自延音 `_`;把“note-onset top-1”当核心指标后才定位到问题。
3. **Keras 3 序列化连环坑**:Transformer 自定义子类保存 `.keras` 前必须先加 `get_config()`,类名不能以下划线开头(scope 名非法),加载时还需 `custom_object_scope`/先 import 注册——每一条都让“训练完 → 复现评测”多踩一次。
4. **评测 prompt 要贴近真实用法**:随机从 test 旋律里切 24 个 token 往往以长音/休止结尾,模型“正确地”立刻收束;改成“以一个新音符结尾、且后面还留 ≥16 token”的窗口后,续写评测才有区分度。
5. **CPU 时间管理**:单 epoch 30~45s,LSTM 与同参数 Transformer 速度几乎一样(oneDNN);真正贵的是“重训 + 重评”的迭代轮数,所以把验证窗口缓存、把生成评测做成固定种子可复现,能显著压缩试错成本。

## 10. 成果文件清单(结果存在哪里)
- 模型与指标:`experiments/{base_s1,cond_s1,base_r1,cond_r1,xf_s1}.keras` + `*_results.json`;`experiments/summary.json`、`experiments/note_metrics.json`;
- 数字表:`REPORT_TABLE.md`;
- 可听 demo:`v3-demo-cond-C-Am-F-G.mid/.wav`、`v3-demo-cond-Am-F-C-G.*`、`v3-demo-cond-C-G-Am-F.*`、`v3-demo-base.*`;原始渲染在 `out/`;
- 人类听感材料:`human_eval/stimuli/{A,B,C}/loop01~04.*`,分析脚本 `human_eval/analysis.py`。
